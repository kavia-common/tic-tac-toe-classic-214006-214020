#!/usr/bin/env sh
# Expo-managed project: native projects and the real Gradle wrapper are generated via `expo prebuild`.
# This stub prevents CI steps that blindly run `./gradlew` from failing due to a missing file.

echo "gradlew not available at repo root. Run 'cd tic_tac_toe_frontend && npm run prebuild:android' to generate Android project and Gradle wrapper."
exit 0
